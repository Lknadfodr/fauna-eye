services:
  #mqtt:
  #  image: eclipse-mosquitto
  #  container_name: mqtt
  #  ports:
  #    - "1883:1883"
  #  volumes:
  #    - ./mosquitto:/mosquitto:rw
  #  user:
  #    felix:felix
  #  restart: unless-stopped
  
  upload:
    build:
      context: "../upload-service"
    image: upload-service:latest
    volumes:
      - ./images/:/app/images
    user: "${PUID}:${PGID}"
    ports:
      - "50100:8080"
    environment:
      TRUSTED_PROXY: "172.16.3.0/24"

